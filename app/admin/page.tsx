export default function AdminPage() {
  return (
    <main className="pvx-boot-screen">
      <div className="pvx-boot-card">
        <h1>Admin Route Placeholder</h1>
        <p>Phase A only creates the App Router surface. Admin dashboard migration starts in a later phase.</p>
      </div>
    </main>
  );
}
